[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-address"
version = "0.1.0"
description = "Room navigation protocol — breadcrumb trails, addressing, path finding"
license = {text = "MIT"}
requires-python = ">=3.8"
